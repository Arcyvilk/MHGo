{
  "name": "mhgo",
  "private": true,
  "scripts": {
    "dev:admin": "turbo run dev:admin",
    "dev:client": "turbo run dev:client",
    "dev:server": "turbo run dev:server",
    "build:admin": "turbo run build:admin",
    "build:client": "turbo run build:client",
    "build:server": "turbo run build:server",
    "build:packages": "turbo run build:package",
    "build:prod": "yarn build:packages --force && yarn build:server --force && yarn build:admin --force && yarn build:client --force"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "eslint-config-mme": "^1.0.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.0.1",
    "nodemon": "^3.0.2",
    "prettier": "^3.1.1",
    "typescript": "*",
    "vite": "^5.0.8",
    "vite-plugin-node": "^3.1.0",
    "vite-plugin-pwa": "^0.17.4",
    "vite-plugin-svgr": "^4.2.0"
  },
  "packageManager": "yarn@1.22.19"
}
